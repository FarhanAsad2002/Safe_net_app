import 'package:flutter/material.dart';

class MonitoringPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Monitoring Activities'),
      ),
      body: Center(
        child: Text('Activity monitoring information will appear here.'),
      ),
    );
  }
}
